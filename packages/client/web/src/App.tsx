import api from '@rpg-tools/config-axios';
import React, { useEffect } from 'react';

const App: React.FC = () => {
  /* useEffect(() => {
    api.get('/').then((response) => {
      console.log(response);
    });
  }, []); */

  return <h1>Hello World</h1>;
};

export default App;
